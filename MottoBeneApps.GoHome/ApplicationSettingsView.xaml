<UserControl x:Class="MottoBeneApps.GoHome.ApplicationSettingsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:goHome="clr-namespace:MottoBeneApps.GoHome"
             mc:Ignorable="d"
             d:DesignHeight="300" d:DesignWidth="300"
             d:DataContext="{d:DesignInstance goHome:ApplicationSettingsViewModel}">
    <Grid>
        <CheckBox IsChecked="{Binding StartWhenWindowsStarts}">Start when windows starts</CheckBox>
    </Grid>
</UserControl>